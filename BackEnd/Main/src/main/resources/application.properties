spring.application.name=Main
server.port=8087

# → Conexão “normal” ao seu DB-alvo (ex: rede_social)
spring.datasource.url=jdbc:postgresql://localhost:5432/redesocialbd
spring.datasource.username=postgres
spring.datasource.password=Enquebravel11
spring.datasource.driver-class-name=org.postgresql.Driver

# → Hibernate criará/atualizará o schema automaticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=always

# Permitir conexões de qualquer IP
server.address:0.0.0.0
# tirar isso depois cacete.


# Desativa a página de erro branca padrão do Spring Boot
# Define o caminho para a página de erro
# Mostra detalhes do erro (útil para desenvolvimento, desative em produção)
# server.error.whitelabel.enabled=false
# server.error.path=/error
# server.error.include-message=always
# server.error.include-stacktrace=never


# coisas que eu vi no desafio do banco itau.
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=postgres #seu nome no PGadmin
# spring.datasource.password=postgres #sua senha no PGadmin
# spring.datasource.url=jdbc:postgresql://localhost:5432/bancocnab # urln do banco de dados
# spring.jpa.generate-ddl=true
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
